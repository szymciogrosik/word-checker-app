export class ExcelQuestion {
  questionKey: string;
  questionValue: string;

  constructor(questionKey: string, questionValue: string) {
    this.questionKey = questionKey;
    this.questionValue = questionValue;
  }
}
